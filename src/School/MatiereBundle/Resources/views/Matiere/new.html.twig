{% extends 'SchoolGestionBundle:Default:index.html.twig' %}

{% block content -%}
    <h2 class="text-center">Nouvelle matière</h2>

    {#{{ form(form) }}#}


    {{ form_start(form)}}
    <div class="form-group">
        <div class="row">
            <div class="col-md-offset-4 col-sm-3">
                <label>Nom de la mati&egrave;re</label>
                {{ form_widget(form.nom, {'attr': {'class': 'form-control', 'placeholder':'ex: Informatique'}})  }}
            </div>
        </div>
    </div>
    <div class="form-group">
        <div class="row">
            <div class="col-md-offset-4 col-sm-3">
                <label>Abr&eacute;viation</label>
                {{ form_widget(form.abreviation, {'attr': {'class': 'form-control', 'placeholder':'ex: INFO'}})  }}
            </div>
        </div>
    </div>
    <div class="form-group">
        <div class="row">
            <div class="col-md-offset-4 col-sm-3">
                <label>Cat&eacute;gorie</label>
                {{ form_widget(form.categorie, {'attr': {'class': 'form-control'}})  }}
            </div>
        </div>
    </div>
    {{ form_end(form)}}
    <ul class="record_actions">
        <li>
            &nbsp;<a class="btn btn-default" href="{{ path('matiere') }}">
                Annuler
            </a>
        </li>
    </ul>
{% endblock %}
