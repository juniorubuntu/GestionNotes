{% extends 'SchoolGestionBundle:Default:index.html.twig' %}

{% block content -%}
    <h1 class="text-center">D&eacute;tails sur une Classe</h1>

    <table class="record_properties table table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
            <tr>
                <th>Classe M&egrave;re</th>
                <td>{{ entity.classePere }}</td>
            </tr>
            <tr>
                <th>Abr&eacute;viation</th>
                <td>{{ entity.abreviation }}</td>
            </tr>
        </tbody>
    </table>
    <table>
        <tr>
            <td>
                <a href="{{ path('classe')}}" class="btn btn-primary">
                    <i class="glyphicon glyphicon-chevron-left"></i>
                    Retour &agrave; la liste
                </a>
            </td>
            <td>&nbsp;&nbsp;</td>
            <td>
                <a href="{{ path('classe_edit',{'id': entity.id}) }}" class="btn btn-success">
                    <i class="glyphicon glyphicon-edit"> </i>
                    Modifier
                </a>
            </td>
            <td><td>&nbsp;&nbsp;</td></td>
            <td>
                {{ form(delete_form) }}
            </td>
        </tr>
    </table>
{% endblock %}
