{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Evaluation</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Note</th>
                <td>{{ entity.note }}</td>
            </tr>
            <tr>
                <th>Anneedebut</th>
                <td>{{ entity.anneeDebut|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Anneefin</th>
                <td>{{ entity.anneeFin|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ entity.annee }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('evaluation') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('evaluation_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
